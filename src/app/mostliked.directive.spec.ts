import { MostlikedDirective } from './mostliked.directive';

describe('MostlikedDirective', () => {
  it('should create an instance', () => {
    const directive = new MostlikedDirective();
    expect(directive).toBeTruthy();
  });
});
